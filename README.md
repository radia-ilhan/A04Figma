1. getElementById: gets 1 element by id

getElementsByClassName: gets many elements by class name 

querySelector: gets first matching element 

querySelectorAll: gets all matching elements 

2. using these : document.createElement("p")

element.innerText = "Radia"

parent.appendChild(element)

3. event going upwards in the serial child, parent, body

4. use of bubbling to handle child by parent. Faster, efficient, less manual 

5. preventDefault() stops normal action, stopPropagation() stops  event going to parent
